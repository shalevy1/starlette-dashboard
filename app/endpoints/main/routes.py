# -*- coding: utf-8 -*-
# from starlette.routing import Route, Mount
# from starlette.staticfiles import StaticFiles
# from endpoints.main import endpoints as page_endpoints


# routes = [
#     Route("/", endpoint=endpoints.homepage, methods=["GET"]),
#     Route("/{page}", endpoint=endpoints.homepage_page, methods=["GET"]),
#     # Route("/index2", endpoint=endpoints.callback, methods=["GET"]),
#     Mount("/static", app=StaticFiles(directory="statics"), name="static"),
# ]
